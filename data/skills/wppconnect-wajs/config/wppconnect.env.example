# WPPConnect WA-JS config (example)
# Copy to: config/wppconnect.env

# Docker image (override if needed)
WPP_IMAGE=mathiasvinicius/wppconnect:latest

# Host port exposed for the WA-JS HTTP API
WPP_PORT=21465

# Optional base URL (if not localhost)
# WPP_BASE_URL=http://127.0.0.1:21465

# Optional secret key for token generation
# WPP_SECRETKEY=change-me
